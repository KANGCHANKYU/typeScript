{
    "compilerOptions": {
        "outDir": "dist", // 변환된 js 파일을 모아두는 경로
        "target": "ES6" , // js 파일 버전
        "module": "CommonJS", //  환경에 따라 사용하는 js가 다른데 현재 CommonJs의 경우 node에서 사용하는 js 
        "lib": ["ES6"], // 어떤 버전에서 사용할 라이브러리인가 설정하는곳
    "sourceMap": true 
    },
}